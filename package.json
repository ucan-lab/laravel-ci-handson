{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build"
    },
    "devDependencies": {
        "axios": "^1.7.4",
        "laravel-vite-plugin": "^1.0",
        "vite": "^5.0"
    },
    "engines": {
        "node": ">=20.17.0",
        "npm": "Don't use yarn, Use pnpm",
        "yarn": "Don't use yarn, Use pnpm"
    }
}
